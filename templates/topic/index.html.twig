{% extends 'base.html.twig' %}



{% block body %}
<a class="btn btn-sm btn-primary"href="{{path(('topic_add'))}}">Ajout d'un topic</a>
<table class="table table-striped table-bordered">
  {% for topic in topics %}
  <tr>
    <td>{{topic.name}}</td>
    <td><a href="{{path('topic_delete', {'id': topic.id})}}"class="btn btn-sm btn-danger">Suppr</button></td>

  </tr>
  {% endfor %}
</table>

{% endblock %}
